#ifndef GLOBALS_H
#define GLOBALS_H

enum class AngleMode { Degrees, Radians };

// Global angle mode (default to Degrees)
extern AngleMode currentAngleMode;

// You can add more global settings here as needed.
// For example:
// extern int someOtherGlobalSetting;

#endif
